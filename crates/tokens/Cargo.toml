[package]
name = "tokens"
authors = ["Paul Lavender-Jones <paul@lavender-jones.com>"]
version = "0.1.0"
edition = "2024"
license = "MIT"
build = "build.rs"

[dependencies]
logos = "0.16.0"

[build-dependencies]
toml = "0.9.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
