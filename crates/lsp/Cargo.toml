[package]
name = "lsp"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
tower-lsp = "0.20"
tokio = { version = "1", features = ["rt-multi-thread", "io-std", "macros"] }
miette = "7.6.0"

# Internal crates
analyse = { path = "../analyse" }
