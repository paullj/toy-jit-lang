[package]
name = "lsp"
version = "0.1.0"
edition = "2024"

[dependencies]
tower-lsp = "0.20"
tokio = { version = "1", features = ["rt-multi-thread", "io-std", "macros"] }
miette = "7.6.0"

# Internal crates
syntax = { path = "../syntax" }
parse = { path = "../parse" }
ast = { path = "../ast" }
hir = { path = "../hir" }
infer = { path = "../infer" }
