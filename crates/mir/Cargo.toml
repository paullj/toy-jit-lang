[package]
name = "mir"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hir = { path = "../hir" }
infer = { path = "../infer" }

[dev-dependencies]
ast = { path = "../ast" }
la-arena = "0.3.1"
parse = { path = "../parse" }
