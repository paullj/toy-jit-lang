[package]
name = "runtime"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
mir = { path = "../mir" }
jit = { path = "../jit" }
compile = { path = "../compile" }
vm = { path = "../vm" }
infer = { path = "../infer" }
hir = { path = "../hir" }
lasso = "0.7"
thiserror = "2.0"
