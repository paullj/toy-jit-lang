import Root, {
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	buttonVariants,
} from "./button.svelte";

export {
	Root,
	type ButtonProps as Props,
	type ButtonSize,
	type ButtonVariant,
	Root as Button,
	buttonVariants,
};
